---
neo4j:
  epic: "Epic:DMT:EPIC-DMT_METADATA"
feishu:
  wiki: "V8KEfpg1vlkCZld"
doc:
  title: "PRD-PD-DMT-元数据管理-v3.1.md"
  version: "v1.0"
  type: "PRD"
  productDomain: "PD-DMT"
  epicKey: "Epic:DMT:EPIC-DMT_METADATA"
  author: "Tony Stark"
  createdAt: "2026-04-29"
  updatedAt: "2026-04-29"
---

# PRD-PD-DMT-META - 元数据管理需求文档

> **版本**: v3.1
> **日期**: 2026-04-28
> **作者**: Tony Stark
> **状态**: 待评审

---

## 一、元数据

```yaml
---
产品域: PD-DMT - 数据管理
Epic: EPIC-DMT_METADATA_MANAGE - 元数据管理
Epic URI: Epic:EPIC-DMT-METADATA-MANAGE
PRD 版本: v3.1
创建日期: 2026-04-28
审核人: 待定
产品经理: Tony Stark
---
```

---

## 二、变更日志

| 时间 | 版本号 | 变更人 | 主要变更内容 |
|:---|:---:|:---|:---|
| 2026-04-27 | 2.0.0 | Tony Stark | 基于Epic说明文档v2.0重建，对齐PRD模板v5.0，Story按Feature展开 |
| 2026-04-28 | 3.0.0 | Tony Stark | 模板格式优化，完善Story格式，增加交互说明，修正FP关联表格式 |
| 2026-04-28 | 3.1.0 | Tony Stark | 融合钟离功能说明文档，细化元数据建模/采集功能详情，补充Tab结构、字段定义、Mock数据、页面布局图示，增加元数据管理关系图和任务状态规范附录 |

---

## 三、需求背景

### 3.1 目标

元数据管理系统提供企业级元数据的统一建模、采集、存储与检索能力，支持数据资产、要素、资源的上下架管理，构建完整的元数据资产视图。

### 3.2 痛点

1. **元数据分散**：各系统元数据散落，缺乏统一建模和管理入口
2. **采集成本高**：元数据采集依赖人工维护，成本高且容易遗漏
3. **上下架混乱**：资产、要素、资源缺乏统一的上下架管理机制
4. **标签缺失**：元数据缺乏标签体系，检索和分类困难

### 3.3 核心挑战

1. 多源异构元数据的自动采集与标准化
2. 上下架审批流程与数据资产联动
3. 标签体系的灵活扩展与继承

---

## 四、需求范围

### 4.1 功能范围

本次需求聚焦元数据管理，核心覆盖 6 个 Feature：

| Feature ID | Feature 名称 | 优先级 | 状态 |
|:---|:---|:---:|:---:|
| FEAT-DMT-METADATA-MODELING | 元数据建模 | P0 | 待开发 |
| FEAT-DMT-METADATA-COLLECT | 元数据采集 | P0 | 待开发 |
| FEAT-DMT-ASSET-MANAGE-TAG | 标签管理 | P1 | 待开发 |
| FEAT-DMT-ELEMENT-LISTING | 要素上下架 | P1 | 待开发 |
| FEAT-DMT-ASSET-LISTING | 资产上下架 | P1 | 待开发 |
| FEAT-DMT-RESOURCE-LISTING | 资源上下架 | P1 | 待开发 |

### 4.2 不在本次范围内

- 元数据血缘分析（属于数据发现域）
- 元数据质量检测（属于数据标准域）
- 自动采集任务调度（可作为后续迭代）

---

## 五、功能详情

---

### Feature 1：元数据建模 {#F-001}

**类型**: 🆕 新增

**功能描述**：提供元数据模型的创建、编辑和版本管理能力，支持业务概念模型和技术元数据模型的统一管理。

**关联 Story**：

| Story ID | Story 名称 | 优先级 | 状态 |
|:---|:---|:---:|:---:|
| Story-META-001 | 创建元数据模型 | P0 | 待开发 |
| Story-META-002 | 编辑元数据模型 | P0 | 待开发 |
| Story-META-003 | 模型版本管理 | P0 | 待开发 |
| Story-META-004 | 模型发布 | P1 | 待开发 |

---

#### Story-META-001：创建元数据模型

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据架构师，需要创建新的元数据模型，定义模型名称、字段结构、数据类型，以便后续数据资产能够挂接该模型 |
| 优先级 | P0 |
| 状态 | 待开发 |

**验收标准**：

1. 支持新建模型，填写模型名称、模型类型（业务模型/技术模型）、描述
2. 支持添加字段：字段名、字段类型、是否必填、默认值、注释
3. 支持字段拖拽排序
4. 模型保存前进行必填校验，未填写完整禁止保存
5. 保存成功后提示「模型创建成功」，自动跳转模型详情页

**交互说明**：

模型列表页点击「新建模型」按钮；弹出模型创建表单Dialog；字段列表支持动态添加行；字段类型下拉可选（STRING/INT/BIGINT/DECIMAL/DATE/DATETIME/JSON/ARRAY）；点击「保存」触发表单校验；校验失败在对应字段下方展示错误提示。

**页面布局图示**：
```
┌─────────────────────────────────────────────────────────┐
│ 页面标题：元数据建模                                      │
│ 描述：构建元数据模型，定义实体绑定、血缘关系与数据标准映射  │
├─────────────────────────────────────────────────────────┤
│ [实体绑定] [血缘构建] [标准映射]                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│ Tab 内容区域（根据选中Tab展示对应内容）                    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

#### Story-META-002：编辑元数据模型

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据架构师，需要编辑已有元数据模型的字段结构或属性，保持模型与实际数据源结构一致 |
| 优先级 | P0 |
| 状态 | 待开发 |

**验收标准**：

1. 打开已有模型，进入编辑模式后可修改模型名称、描述、字段结构
2. 支持增加、删除、修改字段
3. 支持修改字段类型，修改时提示「字段类型变更可能导致数据丢失」
4. 保存前进行必填校验
5. 编辑操作记录操作日志，包含操作人、操作时间、变更内容

**交互说明**：

模型详情页右上角「编辑」按钮；进入编辑模式后所有字段变为可编辑状态；删除字段时弹出确认Dialog「删除字段将同时删除该模型下所有资产的该字段数据，确认删除？」；保存成功后退出编辑模式，页面刷新为最新数据。

---

#### Story-META-003：模型版本管理

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据架构师，需要查看元数据模型的历史版本，对比不同版本之间的差异，并在必要时回滚到旧版本 |
| 优先级 | P0 |
| 状态 | 待开发 |

**验收标准**：

1. 模型详情页展示版本历史列表（版本号、创建时间、创建人）
2. 支持选择历史版本进行对比（展示差异高亮）
3. 支持回滚到指定历史版本（需二次确认）
4. 回滚操作生成新版本号，不删除原有版本
5. 每次版本变更记录变更日志

**交互说明**：

模型详情页左侧「版本历史」Tab；版本列表按时间倒序；点击版本号展开该版本详情；「对比」按钮选择两个版本进行差异对比（新增字段绿色、删除字段红色、修改字段黄色）；「回滚」按钮在二次确认后执行并生成新版本。

---

#### Story-META-004：模型发布

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据架构师，模型设计完成后提交发布申请，审批通过后模型正式生效，可被数据资产引用 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 模型创建/编辑完成后可提交发布申请
2. 发布前展示待发布内容预览
3. 发布申请提交后状态变为「待发布」
4. 审批通过后模型状态变为「已发布」，可被资产引用
5. 已发布的模型变更需提交新版本，不可直接覆盖

**交互说明**：

模型编辑页/创建页底部「提交发布」按钮；点击后弹出发布预览Dialog，展示模型内容和变更说明；确认后提交申请；申请提交后按钮变为「查看申请状态」；审批通过后模型卡片状态标签变更为「已发布」。

---

### Feature 2：元数据采集 {#F-002}

**类型**: 🆕 新增

**功能描述**：支持多数据源的元数据自动采集和手动录入，提供采集任务配置、执行和结果查看能力。

**关联 Story**：

| Story ID | Story 名称 | 优先级 | 状态 |
|:---|:---|:---:|:---:|
| Story-META-005 | 新建采集任务 | P0 | 待开发 |
| Story-META-006 | 采集任务执行 | P0 | 待开发 |
| Story-META-007 | 采集结果查看 | P0 | 待开发 |
| Story-META-008 | 手动录入元数据 | P1 | 待开发 |

---

#### Story-META-005：新建采集任务

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师，需要配置数据源的元数据采集任务，设置采集频率和数据范围，定期自动采集元数据 |
| 优先级 | P0 |
| 状态 | 待开发 |

**验收标准**：

1. 支持选择数据源类型：MySQL / PostgreSQL / Oracle / Hive / API
2. 填写采集配置：连接信息（主机/端口/数据库/用户名/密码）、采集范围（指定表或全部）
3. 支持设置采集频率：立即执行 / 每天 / 每周 / 自定义Cron
4. 密码字段加密存储
5. 采集任务创建后自动测试连接，连接失败禁止保存

**交互说明**：

采集任务列表页点击「新建采集任务」；弹出任务配置Dialog；数据源类型下拉选择后动态展示对应连接字段；连接测试按钮在填写完连接信息后可用；测试成功后按钮变绿并显示「连接成功」；采集频率选择器提供预设选项和自定义Cron输入框。

**页面布局图示**：
```
┌─────────────────────────────────────────────────────────┐
│ 页面标题：元数据采集                          [新建] [刷新]│
├─────────────────────────────────────────────────────────┤
│ [数据抽取任务] [同步任务]                                 │
├─────────────────────────────────────────────────────────┤
│ 🔍 搜索任务名称 | 数据源类型 ▼ | 运行状态 ▼               │
├─────────────────────────────────────────────────────────┤
│                                                          │
│ 表格内容区域                                              │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

#### Story-META-006：采集任务执行

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师，配置好采集任务后执行任务，查看元数据采集进度和执行结果，及时发现采集异常 |
| 优先级 | P0 |
| 状态 | 待开发 |

**验收标准**：

1. 支持手动触发采集任务执行
2. 执行过程展示进度条（已采集表数/总表数）
3. 支持查看执行日志，包含每张表的采集状态
4. 执行失败展示错误原因，支持重试
5. 定时任务按配置频率自动执行

**交互说明**：

任务列表页行操作列「立即执行」按钮；点击后按钮变为Loading状态并展示进度条；任务执行完成后弹出结果Dialog展示采集统计（成功N个/失败N个）；点击「查看日志」进入执行日志页面；执行日志按表名展示状态和耗时。

---

#### Story-META-007：采集结果查看

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师/数据分析师，在采集完成后查看元数据的采集结果，确认数据表、字段、类型是否正确 |
| 优先级 | P0 |
| 状态 | 待开发 |

**验收标准**：

1. 采集结果以表维度展示，每张表显示表名、行数、字段数、最后采集时间
2. 支持点击表名查看字段级元数据详情（字段名/类型/注释）
3. 支持对采集结果进行确认或标记异常
4. 异常表可申请重新采集
5. 采集结果支持导出为 Excel

**交互说明**：

采集结果页以列表形式展示采集到的表；每行右侧有「查看详情」和「重新采集」操作；点击表名展开字段级详情；字段类型旁有关联的标准信息（标准字典）；确认/异常按钮在详情页底部；导出按钮在页面右上角。

---

#### Story-META-008：手动录入元数据

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师，对于无法自动采集的数据源，手动录入元数据信息，保证元数据覆盖完整 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 支持手动录入数据表和字段级元数据
2. 录入时选择挂接的元数据模型
3. 支持批量导入（Excel模板），Excel格式包含：表名、字段名、字段类型、注释
4. 导入前进行格式校验，格式错误行高亮提示
5. 导入完成后展示导入结果（成功/失败数量）

**交互说明**：

元数据列表页点击「手动录入」或「批量导入」；手动录入入口为创建表单；批量导入入口为文件上传Dialog；Excel上传后展示预览数据（表格形式）；错误行以红色高亮并标注错误原因；确认导入后弹出导入结果。

---

### Feature 3：标签管理 {#F-003}

**类型**: 🆕 新增

**功能描述**：提供元数据的标签体系管理，包括标签定义、标签分配、标签检索和标签统计。

**关联 Story**：

| Story ID | Story 名称 | 优先级 | 状态 |
|:---|:---|:---:|:---:|
| Story-META-009 | 标签定义 | P1 | 待开发 |
| Story-META-010 | 标签分配 | P1 | 待开发 |
| Story-META-011 | 标签检索 | P1 | 待开发 |

---

#### Story-META-009：标签定义

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据管理员，需要定义业务标签体系（敏感等级/数据域/业务主题等），为后续标签分配提供标签池 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 支持创建标签：标签名称、标签类型（敏感等级/数据域/业务主题/自定义）、描述
2. 支持标签分类管理
3. 支持编辑、禁用、删除标签（禁用后已分配的标签保留）
4. 标签名称不允许重复
5. 系统内置标签不可删除

**交互说明**：

标签管理页展示所有标签列表（支持按类型筛选）；点击「新建标签」弹出创建Dialog；标签类型选择后动态展示对应配置字段；标签列表每行右侧有编辑/禁用/删除操作；删除时判断该标签是否已被使用，被使用则提示「该标签已被N个资产使用，删除后将一并移除，确认删除？」。

---

#### Story-META-010：标签分配

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师/数据分析师，为元数据分配标签，便于后续按标签检索和分类管理 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 资产详情页提供「分配标签」入口
2. 支持多选标签批量分配
3. 已分配的标签以标签气泡形式展示，支持单独移除
4. 标签分配操作实时保存
5. 支持批量分配（勾选多个资产后点击「批量打标签」）

**交互说明**：

资产详情页「标签」区域；点击「+添加标签」弹出标签选择Dialog（搜索 + 多选Checkbox）；已分配标签以彩色气泡展示；鼠标悬停气泡显示移除x号；批量选择模式下页面顶部出现批量操作栏（批量打标签/批量移除标签）。

---

#### Story-META-011：标签检索

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据分析师，通过标签快速筛选目标元数据，找到特定业务主题或敏感等级下的所有数据资产 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 元数据列表页支持按标签筛选（多选）
2. 标签筛选与关键词搜索组合（AND关系）
3. 筛选条件可保存为常用筛选器
4. 筛选结果展示时高亮匹配的标签
5. 支持按标签统计资产数量分布

**交互说明**：

元数据列表页左侧筛选面板包含「标签筛选」Tab；标签以树形或Checkbox列表形式展示（支持多选）；选中标签后列表实时刷新；顶部筛选标签栏展示当前选中标签（可单点移除）；「保存筛选器」入口在筛选栏最右侧。

---

### Feature 4：要素上下架 {#F-004}

**类型**: 🆕 新增

**功能描述**：管理数据要素的上下架状态，支持要素的上架申请、审批、下架操作及状态跟踪。

**关联 Story**：

| Story ID | Story 名称 | 优先级 | 状态 |
|:---|:---|:---:|:---:|
| Story-META-012 | 要素上架申请 | P1 | 待开发 |
| Story-META-013 | 要素审批 | P1 | 待开发 |
| Story-META-014 | 要素下架 | P1 | 待开发 |

---

#### Story-META-012：要素上架申请

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师，需要将新创建的要素提交上架申请，通过审批后要素对其他用户可见 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 要素列表页支持「提交上架」操作
2. 上架申请需填写：上架理由、业务说明、关联资产
3. 提交后状态变为「待审批」
4. 申请人可查看申请进度和审批结果
5. 上架成功后要素状态变为「已上架」

**交互说明**：

要素详情页底部「提交上架」按钮；点击后弹出上架申请Dialog（表单：理由/业务说明/关联资产）；关联资产支持多选搜索；提交后按钮变为「查看申请状态」；申请状态变化时站内通知申请人。

---

#### Story-META-013：要素审批

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据管理员，审批要素上架申请，确认要素信息准确、合规后通过或拒绝 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 审批列表展示所有待审批要素申请
2. 审批详情页展示要素信息和上架理由
3. 支持审批通过/拒绝，拒绝需填写原因
4. 审批通过后要素状态变为「已上架」
5. 审批拒绝后申请人收到站内通知（包含拒绝原因）

**交互说明**：

审批列表入口在管理员后台（左侧菜单「要素审批」）；列表展示要素名称、申请人、申请时间；点击「审批」进入审批详情页；页面底部通过/拒绝按钮；拒绝时弹出原因填写Dialog（必填）；通过后要素自动上架。

---

#### Story-META-014：要素下架

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师/管理员，当要素不再需要对外服务时，提交下架申请，下架后要素从列表隐藏 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 已上架要素支持「提交下架」操作
2. 下架申请需填写下架原因（必填）
3. 下架前检查该要素是否被其他资产引用，被引用时提示「该要素被N个资产引用，下架将影响下游，确认下架？」
4. 下架成功后要素状态变为「已下架」，从已上架列表移除
5. 下架历史保留，可查询历史下架记录

**交互说明**：

要素详情页底部「提交下架」按钮（仅已上架要素可见）；点击后弹出下架申请Dialog；下架原因文本框（必填，最少10字）；提交后弹出引用检查结果；确认下架后要素状态更新。

---

### Feature 5：资产上下架 {#F-005}

**类型**: 🆕 新增

**功能描述**：管理数据资产的上下架状态，支持资产的上架申请、审批、下架操作及状态跟踪。

**关联 Story**：

| Story ID | Story 名称 | 优先级 | 状态 |
|:---|:---|:---:|:---:|
| Story-META-015 | 资产上架申请 | P1 | 待开发 |
| Story-META-016 | 资产审批 | P1 | 待开发 |
| Story-META-017 | 资产下架 | P1 | 待开发 |

---

#### Story-META-015：资产上架申请

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师，需要将新创建的数据资产提交上架申请，通过审批后资产对其他用户可见并可被检索 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 资产列表页支持「提交上架」操作
2. 上架申请需填写：上架理由、业务说明、关联要素
3. 提交后状态变为「待审批」
4. 申请人可查看申请进度和审批结果
5. 上架成功后资产状态变为「已上架」，出现在资产列表和搜索结果中

**交互说明**：

资产详情页底部「提交上架」按钮；点击后弹出上架申请Dialog；关联要素支持多选搜索；提交后按钮变为「查看申请状态」；申请状态变化时站内通知申请人。

---

#### Story-META-016：资产审批

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据管理员，审批数据资产上架申请，确认资产信息准确、符合规范后通过或拒绝 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 审批列表展示所有待审批资产申请
2. 审批详情页展示资产信息和上架理由
3. 支持审批通过/拒绝，拒绝需填写原因
4. 审批通过后资产状态变为「已上架」
5. 审批拒绝后申请人收到站内通知

**交互说明**：

审批列表入口在管理员后台（左侧菜单「资产审批」）；列表展示资产名称、申请人、申请时间；点击「审批」进入审批详情页；页面底部通过/拒绝按钮；拒绝时弹出原因填写Dialog；通过后资产自动上架。

---

#### Story-META-017：资产下架

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师/管理员，当数据资产不再需要对外服务时，提交下架申请，下架后资产从列表和搜索结果中移除 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 已上架资产支持「提交下架」操作
2. 下架申请需填写下架原因（必填）
3. 下架前检查该资产是否被其他资产引用，被引用时提示引用关系
4. 下架成功后资产状态变为「已下架」，从列表移除
5. 下架历史保留，可查询历史下架记录

**交互说明**：

资产详情页底部「提交下架」按钮（仅已上架资产可见）；点击后弹出下架申请Dialog；下架原因文本框（必填）；提交后弹出引用检查结果；确认下架后资产状态更新。

---

### Feature 6：资源上下架 {#F-006}

**类型**: 🆕 新增

**功能描述**：管理数据资源的上下架状态，支持资源的申请、审批、下架操作及状态跟踪。

**关联 Story**：

| Story ID | Story 名称 | 优先级 | 状态 |
|:---|:---|:---:|:---:|
| Story-META-018 | 资源上下架管理 | P1 | 待开发 |
| Story-META-019 | 资源状态查询 | P1 | 待开发 |

---

#### Story-META-018：资源上下架管理

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据工程师，需要管理数据资源的上下架状态，提交上架申请或下架申请，确保资源状态与实际服务状态一致 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 资源列表页支持按状态（已上架/待审批/已下架）筛选
2. 资源详情页展示当前状态和状态变更历史
3. 上架/下架申请流程与要素/资产类似
4. 状态变更后自动通知相关用户
5. 资源上下架需记录操作人和操作时间

**交互说明**：

资源列表页左侧状态筛选Tab；资源详情页右侧为状态变更时间轴（每次状态变更记录时间/操作人/变更原因）；上架/下架申请入口在详情页底部。

---

#### Story-META-019：资源状态查询

| 字段 | 内容 |
|:---|:---|
| 角色-场景-价值 | 作为数据分析师/数据工程师，需要查询资源当前状态，了解资源是否可用以及最后变更时间 |
| 优先级 | P1 |
| 状态 | 待开发 |

**验收标准**：

1. 资源列表页每行展示资源名称、类型、状态、最后变更时间
2. 状态以彩色标签展示：已上架（绿色）/ 待审批（橙色）/ 已下架（灰色）
3. 支持按资源名称关键词搜索
4. 支持按状态、类型、变更时间筛选
5. 资源状态变更时更新最后变更时间

**交互说明**：

资源列表页（/discovery/data-resources/list）顶部搜索框和筛选栏；列表按最后变更时间倒序；状态标签在每行资源名称后以彩色小标签展示；搜索支持实时防抖过滤。

---

## 六、Story 与 FP 关联表

> 本期需求聚焦 Story 层面交付，暂不拆分 FP。如后续需要精细化开发管理，可按 Story 进一步拆解 FP。

### Feature 1 关联 Story

| Story ID | Story 名称 | 对应 FP | 优先级 |
|:---|:---|:---|:---:|
| Story-META-001 | 创建元数据模型 | FP-META-001 模型创建 | P0 |
| Story-META-002 | 编辑元数据模型 | FP-META-002 模型编辑 | P0 |
| Story-META-003 | 模型版本管理 | FP-META-003 版本管理 | P0 |
| Story-META-004 | 模型发布 | FP-META-004 模型发布 | P1 |

### Feature 2 关联 Story

| Story ID | Story 名称 | 对应 FP | 优先级 |
|:---|:---|:---|:---:|
| Story-META-005 | 新建采集任务 | FP-META-005 任务配置 | P0 |
| Story-META-006 | 采集任务执行 | FP-META-006 任务执行 | P0 |
| Story-META-007 | 采集结果查看 | FP-META-007 结果查看 | P0 |
| Story-META-008 | 手动录入元数据 | FP-META-008 手动录入 | P1 |

### Feature 3 关联 Story

| Story ID | Story 名称 | 对应 FP | 优先级 |
|:---|:---|:---|:---:|
| Story-META-009 | 标签定义 | FP-META-009 标签定义 | P1 |
| Story-META-010 | 标签分配 | FP-META-010 标签分配 | P1 |
| Story-META-011 | 标签检索 | FP-META-011 标签检索 | P1 |

### Feature 4 关联 Story

| Story ID | Story 名称 | 对应 FP | 优先级 |
|:---|:---|:---|:---:|
| Story-META-012 | 要素上架申请 | FP-META-012 要素上架 | P1 |
| Story-META-013 | 要素审批 | FP-META-013 要素审批 | P1 |
| Story-META-014 | 要素下架 | FP-META-014 要素下架 | P1 |

### Feature 5 关联 Story

| Story ID | Story 名称 | 对应 FP | 优先级 |
|:---|:---|:---|:---:|
| Story-META-015 | 资产上架申请 | FP-META-015 资产上架 | P1 |
| Story-META-016 | 资产审批 | FP-META-016 资产审批 | P1 |
| Story-META-017 | 资产下架 | FP-META-017 资产下架 | P1 |

### Feature 6 关联 Story

| Story ID | Story 名称 | 对应 FP | 优先级 |
|:---|:---|:---|:---:|
| Story-META-018 | 资源上下架管理 | FP-META-018 上下架管理 | P1 |
| Story-META-019 | 资源状态查询 | FP-META-019 状态查询 | P1 |

---

## 七、FP 清单汇总

| FP ID | 功能点 | 所属 Feature | 所属 Story | 优先级 |
|:---|:---|:---|:---|:---:|:---|
| FP-META-001 | 模型创建 | Feature 1 元数据建模 | Story-META-001 | P0 |
| FP-META-002 | 模型编辑 | Feature 1 元数据建模 | Story-META-002 | P0 |
| FP-META-003 | 版本管理 | Feature 1 元数据建模 | Story-META-003 | P0 |
| FP-META-004 | 模型发布 | Feature 1 元数据建模 | Story-META-004 | P1 |
| FP-META-005 | 任务配置 | Feature 2 元数据采集 | Story-META-005 | P0 |
| FP-META-006 | 任务执行 | Feature 2 元数据采集 | Story-META-006 | P0 |
| FP-META-007 | 结果查看 | Feature 2 元数据采集 | Story-META-007 | P0 |
| FP-META-008 | 手动录入 | Feature 2 元数据采集 | Story-META-008 | P1 |
| FP-META-009 | 标签定义 | Feature 3 标签管理 | Story-META-009 | P1 |
| FP-META-010 | 标签分配 | Feature 3 标签管理 | Story-META-010 | P1 |
| FP-META-011 | 标签检索 | Feature 3 标签管理 | Story-META-011 | P1 |
| FP-META-012 | 要素上架 | Feature 4 要素上下架 | Story-META-012 | P1 |
| FP-META-013 | 要素审批 | Feature 4 要素上下架 | Story-META-013 | P1 |
| FP-META-014 | 要素下架 | Feature 4 要素上下架 | Story-META-014 | P1 |
| FP-META-015 | 资产上架 | Feature 5 资产上下架 | Story-META-015 | P1 |
| FP-META-016 | 资产审批 | Feature 5 资产上下架 | Story-META-016 | P1 |
| FP-META-017 | 资产下架 | Feature 5 资产上下架 | Story-META-017 | P1 |
| FP-META-018 | 上下架管理 | Feature 6 资源上下架 | Story-META-018 | P1 |
| FP-META-019 | 状态查询 | Feature 6 资源上下架 | Story-META-019 | P1 |

---

## 八、菜单映射表

| 功能模块 | 一级菜单 | 二级菜单 | 路由路径 |
|:---|:---|:---|:---|
| 元数据建模 | 数据管理 | 元数据 | /metadata/model |
| 元数据采集 | 数据管理 | 元数据 | /metadata/collect |
| 标签管理 | 数据管理 | 元数据 | /metadata/tag |
| 要素上下架 | 数据管理 | 要素管理 | /metadata/element |
| 资产上下架 | 数据管理 | 资产管理 | /metadata/asset |
| 资源上下架 | 数据管理 | 资源管理 | /metadata/resource |

---

## 九、审批流接入（如有）

| 审批节点 | 审批角色 | 接入条件 | 审批方式 |
|:---|:---|:---|:---|
| 模型发布审批 | 数据管理员 | 提交模型发布申请 | 人工审批 |
| 要素上架审批 | 数据管理员 | 提交要素上架申请 | 人工审批 |
| 资产上架审批 | 数据管理员 | 提交资产上架申请 | 人工审批 |

---

## 十、版本历史

| 版本 | 日期 | 变更人 | 变更内容 |
|:---|:---:|:---|:---|
| 2.0.0 | 2026-04-27 | Tony Stark | 基于Epic说明文档v2.0重建，对齐PRD模板v5.0，6 Feature / 19 Story |
| 3.0.0 | 2026-04-28 | Tony Stark | 模板格式优化，完善Story格式，增加交互说明，修正FP关联表格式 |
| 3.1.0 | 2026-04-28 | Tony Stark | 融合钟离功能说明文档，细化元数据建模/采集功能详情 |

---

## 十一、元数据管理关系

### 11.1 元数据管理整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     元数据管理                           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  元数据采集 ──→ 元数据存储 ──→ 元数据查询                │
│       ↓               ↓               ↓                 │
│       └───────────────┼───────────────┘                 │
│                       ↓                                 │
│                  元数据建模                              │
│                       ↓                                 │
│          ┌────────────┼────────────┐                   │
│          ↓            ↓            ↓                   │
│     实体绑定      血缘构建     标准映射                 │
│          ↓            ↓            ↓                   │
│          └────────────┼────────────┘                   │
│                       ↓                                 │
│                  标准稽核                                │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 11.2 功能模块说明

| 模块 | 说明 | 入口功能 |
|:---|:---|:---|
| 元数据采集 | 从各种数据源抽取元数据，支持Doris、Hive、Oracle、MySQL、Kafka、Excel等 | 数据抽取任务、同步任务 |
| 元数据存储 | 元数据的统一存储和管理 | 系统内部处理 |
| 元数据查询 | 多维度的元数据检索与查询服务 | 关键词搜索、高级筛选 |
| 元数据建模 | 构建元数据模型，建立物理与业务的映射关系 | 实体绑定、血缘构建、标准映射 |
| 标准稽核 | 执行数据标准稽核任务，监控标准落地情况 | 稽核任务配置、执行、结果查看 |

---

## 十二、任务状态规范

### 12.1 任务状态定义

| 状态值 | 显示文本 | 颜色 | 含义 |
|:---|:---|:---:|:---|
| pending | 等待中 | gray | 任务等待执行 |
| running | 执行中 | arcoblue | 任务正在执行 |
| success | 成功 | green | 任务执行成功 |
| failed | 失败 | red | 任务执行失败 |

### 12.2 状态颜色规范

| 颜色标识 | 使用场景 |
|:---|:---|
| gray（灰色） | 等待中、已下架、草稿、已过期 |
| arcoblue（Arcoblue） | 执行中 |
| green（绿色） | 成功、已发布、有效 |
| red（红色） | 失败、无效 |
| orange（橙色） | 待审核、待审批 |

### 12.3 数据标准状态

| 状态值 | 显示文本 | 颜色 | 含义 |
|:---|:---|:---:|:---|
| published | 已发布 | green | 标准已发布生效 |
| draft | 草稿 | orange | 标准处于草稿状态 |
| pending | 待审核 | blue | 等待审核中 |
| active | 有效 | green | 数据有效可用 |
| inactive | 无效 | red | 数据无效不可用 |
| expired | 已过期 | gray | 数据已过期废弃 |

---

## 十三、详细功能页面说明

### 13.1 元数据建模功能详情

#### 13.1.1 实体绑定 Tab

**页面布局**：
```
┌─────────────────────────────────────────────────────────┐
│ 元数据建模 > 实体绑定                                     │
├─────────────────────────────────────────────────────────┤
│ [新建采集任务]                        🔍 搜索 | 筛选 ▼    │
├─────────────────────────────────────────────────────────┤
│ 物理表名 | 所属库 | 绑定业务域 | 绑定业务实体 | 状态 | 操作│
├─────────────────────────────────────────────────────────┤
│ dwd_user_register | user_center_db | 客户域 | 客户 | 已绑定 │解除绑定│
│ dwd_realname_auth | user_center_db | 客户域 | 客户 | 已绑定 │解除绑定│
│ dwd_credit_apply | credit_core_db | 授信域 | 授信申请 | 已绑定 │解除绑定│
│ dwd_loan_drawdown | credit_core_db | 支用域 | 支用单 | 已绑定 │解除绑定│
│ dws_repayment_plan | repayment_db | 还款域 | 还款计划 | 已绑定 │解除绑定│
│ dws_post_loan_monitor | risk_db | 贷后域 | 贷后记录 | 未绑定 │绑定业务实体│
├─────────────────────────────────────────────────────────┤
│                    分页器 (第1页/共1页)                  │
└─────────────────────────────────────────────────────────┘
```

**列表字段说明**：

| 字段名称 | 说明 | 数据类型 |
|:---|:---|:---|
| 物理表名 | 数据库中的物理表名称 | string |
| 所属库 | 物理表所属的数据库 | string |
| 绑定业务域 | 物理表归属的业务领域 | string |
| 绑定业务实体 | 物理表关联的业务实体 | string |
| 状态 | 绑定状态（已绑定/未绑定） | string |

**操作说明**：

| 操作 | 说明 |
|:---|:---|
| 绑定业务实体 | 点击打开绑定弹窗，选择物理表名、所属库、业务域、业务实体 |
| 解除绑定 | 解除物理表与业务实体的绑定关系 |

**绑定业务实体弹窗字段**：

| 字段名称 | 说明 | 必填 |
|:---|:---|:---|
| 物理表名 | 下拉选择 | 是 |
| 所属库 | 下拉选择，支持自定义输入 | 是 |
| 业务域 | 下拉选择 | 是 |
| 业务实体 | 下拉选择，根据业务域动态过滤 | 是 |

**Mock数据示例**：

| 物理表名 | 所属库 | 绑定业务域 | 绑定业务实体 | 状态 |
|:---|:---|:---|:---|:---|
| dwd_user_register | user_center_db | 客户域 | 客户 | 已绑定 |
| dwd_realname_auth | user_center_db | 客户域 | 客户 | 已绑定 |
| dwd_credit_apply | credit_core_db | 授信域 | 授信申请 | 已绑定 |
| dwd_loan_drawdown | credit_core_db | 支用域 | 支用单 | 已绑定 |
| dws_repayment_plan | repayment_db | 还款域 | 还款计划 | 已绑定 |
| dws_post_loan_monitor | risk_db | 贷后域 | 贷后记录 | 未绑定 |

---

#### 13.1.2 血缘构建 Tab

**页面布局**：
```
┌─────────────────────────────────────────────────────────┐
│ 元数据建模 > 血缘构建                         [自动解析] │
├─────────────────────────────────────────────────────────┤
│ [添加关系]                            🔍 搜索 | 筛选 ▼  │
├─────────────────────────────────────────────────────────┤
│ 上游表(Source) | 下游表(Target) | 转换逻辑 | 关系类型 | 操作│
├─────────────────────────────────────────────────────────┤
│ dwd_user_register → dwd_realname_auth | join register_info | Join │查看图谱│
│ dwd_realname_auth → dwd_credit_apply | filter auth_status | Filter │查看图谱│
│ dwd_credit_apply → dwd_loan_drawdown | join credit_info | ETL │查看图谱│
│ dwd_loan_drawdown → dws_repayment_plan | generate plan | Calculation │查看图谱│
│ dws_repayment_plan → dws_post_loan_monitor | aggregate overdue | Aggregation │查看图谱│
├─────────────────────────────────────────────────────────┤
│                    分页器 (第1页/共1页)                  │
└─────────────────────────────────────────────────────────┘
```

**列表字段说明**：

| 字段名称 | 说明 | 数据类型 |
|:---|:---|:---|
| 上游表 (Source) | 血缘关系的上游表 | string |
| 下游表 (Target) | 血缘关系的下游表 | string |
| 转换逻辑 | 表之间的数据转换说明（SQL片段） | string |
| 关系类型 | 血缘关系类型（ETL、Aggregation、Join、Filter、Calculation等） | string |

**操作说明**：

| 操作 | 说明 |
|:---|:---|
| 自动解析血缘 | 触发自动化血缘解析任务（后台运行） |
| 添加关系 | 打开弹窗手动添加血缘关系 |
| 查看图谱 | 跳转到血缘图谱分析页面 |

**添加关系弹窗字段**：

| 字段名称 | 说明 | 必填 | 控件类型 |
|:---|:---|:---:|:---|
| 上游表 | 下拉选择上游物理表 | 是 | 下拉选择 |
| 下游表 | 下拉选择下游物理表 | 是 | 下拉选择 |
| 关系类型 | 下拉选择血缘关系类型 | 是 | 下拉选择 |
| 转换逻辑 | 描述表之间的数据转换逻辑 | 否 | 文本框 |

**关系类型选项**：
- ETL
- Aggregation
- Join
- Filter
- Calculation
- 其他

**Mock数据示例**：

| 上游表(Source) | 下游表(Target) | 转换逻辑 | 关系类型 |
|:---|:---|:---|:---|
| dwd_user_register | dwd_realname_auth | join register_info | Join |
| dwd_realname_auth | dwd_credit_apply | filter auth_status | Filter |
| dwd_credit_apply | dwd_loan_drawdown | join credit_info | ETL |
| dwd_loan_drawdown | dws_repayment_plan | generate plan | Calculation |
| dws_repayment_plan | dws_post_loan_monitor | aggregate overdue | Aggregation |

---

#### 13.1.3 标准映射 Tab

**页面布局**：
```
┌─────────────────────────────────────────────────────────┐
│ 元数据建模 > 标准映射                           [智能匹配]│
├─────────────────────────────────────────────────────────┤
│ [批量修正]                              🔍 搜索 | 筛选 ▼ │
├─────────────────────────────────────────────────────────┤
│ 物理字段 | 所属表 | 映射数据标准 | 一致性状态 | 操作     │
├─────────────────────────────────────────────────────────┤
│ user_id | dwd_user_register | STD_ID_001 用户ID | ✅符合 | 修正映射│
│ id_card | dwd_realname_auth | STD_ID_002 身份证号 | ✅符合 | 修正映射│
│ apply_amount | dwd_credit_apply | STD_AMT_001 申请金额 | ✅符合 | 修正映射│
│ drawdown_amount | dwd_loan_drawdown | STD_AMT_002 支用金额 | ✅符合 | 修正映射│
│ due_amount | dws_repayment_plan | STD_AMT_003 应还金额 | ✅符合 | 修正映射│
│ overdue_days | dws_post_loan_monitor | STD_NUM_001 逾期天数 | ❌不符合| 修正映射│
├─────────────────────────────────────────────────────────┤
│                    分页器 (第1页/共1页)                  │
└─────────────────────────────────────────────────────────┘
```

**列表字段说明**：

| 字段名称 | 说明 | 数据类型 |
|:---|:---|:---|
| 物理字段 | 数据库表中的字段名 | string |
| 所属表 | 字段所属的物理表 | string |
| 映射数据标准 | 字段映射到的数据标准（编号+名称） | string |
| 一致性状态 | 字段是否符合标准（符合/不符合） | string |

**操作说明**：

| 操作 | 说明 |
|:---|:---|
| 智能匹配标准 | 触发智能标准匹配任务，系统自动建立字段与标准的映射关系 |
| 修正映射 | 打开弹窗手动修正标准映射关系 |

**修正映射弹窗字段**：

| 字段名称 | 说明 | 必填 | 控件类型 | 可编辑 |
|:---|:---|:---:|:---:|:---:|
| 物理字段 | 数据库表中的字段名 | 是 | 只读显示 | 否 |
| 所属表 | 字段所属的物理表 | 是 | 只读显示 | 否 |
| 映射数据标准 | 选择映射的数据标准 | 是 | 下拉选择 | 是 |
| 一致性状态 | 选择是否符合标准 | 是 | 单选 | 是 |

**一致性状态选项**：
- 符合
- 不符合

**Mock数据示例**：

| 物理字段 | 所属表 | 映射数据标准 | 一致性状态 |
|:---|:---|:---|:---|
| user_id | dwd_user_register | STD_ID_001 用户ID | 符合 |
| id_card | dwd_realname_auth | STD_ID_002 身份证号 | 符合 |
| apply_amount | dwd_credit_apply | STD_AMT_001 申请金额 | 符合 |
| drawdown_amount | dwd_loan_drawdown | STD_AMT_002 支用金额 | 符合 |
| due_amount | dws_repayment_plan | STD_AMT_003 应还金额 | 符合 |
| overdue_days | dws_post_loan_monitor | STD_NUM_001 逾期天数 | 不符合 |

---

### 13.2 元数据采集功能详情

#### 13.2.1 数据抽取任务 Tab

**页面布局**：
```
┌─────────────────────────────────────────────────────────┐
│ 元数据采集 > 数据抽取任务                                │
├─────────────────────────────────────────────────────────┤
│ [新建] [刷新]                        🔍 搜索 | 数据源 ▼ |状态▼│
├─────────────────────────────────────────────────────────┤
│ 任务名称 | 数据源 | 抽取对象 | 调度策略 | 最近运行时间 |状态│操作│
├─────────────────────────────────────────────────────────┤
│ 营销数据抽取_Doris | Doris | marketing_db.* | 每日 02:00 |成功│执行│详情│删除│
│ 客户信息抽取_Oracle | Oracle | crm_users | 每周一 01:00|成功│执行│详情│删除│
│ 日志数据实时抽取 | Kafka | app_logs | 实时 | 执行中 │执行│详情│删除│
│ 财务报表抽取 | Excel | finance_2023 | 手动 | 失败 │执行│详情│删除│
├─────────────────────────────────────────────────────────┤
│                    分页器 (第1页/共1页)                  │
└─────────────────────────────────────────────────────────┘
```

**列表字段说明**：

| 字段名称 | 说明 | 数据类型 |
|:---|:---|:---|
| 任务名称 | 抽取任务的名称 | string |
| 数据源 | 数据源类型（Doris、Hive、Oracle、MySQL、Kafka、Excel等） | string |
| 抽取对象 | 抽取的数据对象（库名.表名） | string |
| 调度策略 | 任务的执行调度（每日、每周、实时、手动） | string |
| 最近运行时间 | 最后一次执行的时间 | datetime |
| 状态 | 任务状态（pending/running/success/failed） | string |

**操作说明**：

| 操作 | 说明 |
|:---|:---|
| 执行 | 手动触发任务执行 |详情| 查看任务的详细信息 |
| 删除 | 删除任务（需二次确认） |

**数据源类型选项**：
- Doris
- Hive
- Oracle
- MySQL
- Kafka
- Excel
- PostgreSQL
- SQLServer
- 其他

**调度策略选项**：
- 每日
- 每周
- 实时
- 手动

**Mock数据示例**：

| 任务名称 | 数据源 | 抽取对象 | 调度策略 | 最近运行时间 | 状态 |
|:---|:---|:---|:---|:---|:---|
| 营销数据抽取_Doris | Doris | marketing_db.* | 每日 02:00 | 2026-04-28 02:00 | 成功 |
| 客户信息抽取_Oracle | Oracle | crm_users | 每周一 01:00 | 2026-04-27 01:00 | 成功 |
| 日志数据实时抽取 | Kafka | app_logs | 实时 | 2026-04-28 16:25 | 执行中 |
| 财务报表抽取 | Excel | finance_2023 | 手动 | 2026-04-28 10:00 | 失败 |

---

#### 13.2.2 同步任务 Tab

**页面布局**：
```
┌─────────────────────────────────────────────────────────┐
│ 元数据采集 > 同步任务                                    │
├─────────────────────────────────────────────────────────┤
│ [新建] [刷新]                        🔍 搜索 | 同步类型 ▼ |状态▼│
├─────────────────────────────────────────────────────────┤
│ 任务名称 | 源端 | 目标端 | 同步类型 | 调度策略 | 最近运行时间|状态│操作│
├─────────────────────────────────────────────────────────┤
│ 数仓同步_ODS→DWD | ODS_DB | DWD_DB | 全量同步 | 每日 03:00|成功│执行│详情│删除│
│ 标签同步_ES | Tag_Service | ElasticSearch | 增量同步 | 每小时 |执行中│执行│详情│删除│
│ 元数据同步至Atlas | Hive Metastore | Apache Atlas | 增量同步 | 每日 00:00|成功│执行│详情│删除│
├─────────────────────────────────────────────────────────┤
│                    分页器 (第1页/共1页)                  │
└─────────────────────────────────────────────────────────┘
```

**列表字段说明**：

| 字段名称 | 说明 | 数据类型 |
|:---|:---|:---|
| 任务名称 | 同步任务的名称 | string |
| 源端 | 数据来源端 | string |
| 目标端 | 数据目标端 | string |
| 同步类型 | 全量同步/增量同步 | string |
| 调度策略 | 任务的执行调度 | string |
| 最近运行时间 | 最后一次执行的时间 | datetime |
| 状态 | 任务状态 | string |

**同步类型选项**：
- 全量同步
- 增量同步

**Mock数据示例**：

| 任务名称 | 源端 | 目标端 | 同步类型 | 调度策略 | 最近运行时间 | 状态 |
|:---|:---|:---|:---|:---|:---|:---|
| 数仓同步_ODS→DWD | ODS_DB | DWD_DB | 全量同步 | 每日 03:00 | 2026-04-28 03:00 | 成功 |
| 标签同步_ES | Tag_Service | ElasticSearch | 增量同步 | 每小时 | 2026-04-28 16:00 | 执行中 |
| 元数据同步至Atlas | Hive Metastore | Apache Atlas | 增量同步 | 每日 00:00 | 2026-04-28 00:00 | 成功 |

---

🦾 *PD-DMT 元数据管理 PRD v3.1 | Tony Stark*
