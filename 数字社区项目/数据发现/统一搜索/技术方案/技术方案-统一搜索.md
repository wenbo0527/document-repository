# 技术方案 - 统一搜索

**文档版本**: v1.0
**创建日期**: 2026-04-24
**作者**: Tony Stark
**状态**: 正式版

---

## 一、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    前端层 (Vue3 + Element Plus)              │
├─────────────────────────────────────────────────────────────┤
│  搜索框    搜索结果    搜索历史    高级搜索                   │
├─────────────────────────────────────────────────────────────┤
│                    服务层 (Java Spring Boot)                  │
│  搜索服务    索引服务    建议服务    历史服务                │
├─────────────────────────────────────────────────────────────┤
│                    数据层                                     │
│     Elasticsearch    MySQL     Redis                         │
└─────────────────────────────────────────────────────────────┘
```

## 二、技术栈

| 层级 | 技术选型 | 说明 |
|:---|:---|:---|
| 前端 | Vue3 + TypeScript | 组件化开发 |
| 后端 | Java Spring Boot 2.7 | 业务逻辑处理 |
| 搜索引擎 | Elasticsearch | 全文检索引擎 |
| 数据库 | MySQL 8.0 | 搜索历史存储 |
| 缓存 | Redis 6.0 | 热点数据缓存 |

---

## 三、数据库设计

### 3.1 搜索索引表 (search_index)

```sql
-- 索引结构由 Elasticsearch 管理
-- 索引字段：资源名称、描述、类型、标签、创建时间等
```

### 3.2 搜索历史表 (search_history)

```sql
CREATE TABLE search_history (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    keyword VARCHAR(256) NOT NULL,
    search_type VARCHAR(32) COMMENT 'SEARCH/ADVANCED',
    result_count INT COMMENT '结果数量',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_keyword (keyword),
    INDEX idx_create_time (create_time)
) COMMENT='搜索历史表';
```

### 3.3 热门搜索表 (hot_search)

```sql
CREATE TABLE hot_search (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    keyword VARCHAR(256) NOT NULL,
    search_count INT DEFAULT 0,
    status VARCHAR(32) DEFAULT 'ACTIVE',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_keyword (keyword)
) COMMENT='热门搜索表';
```

---

## 四、接口设计

| 接口 | 方法 | 说明 |
|:---|:---:|:---|
| `/api/v1/search` | GET | 执行搜索 |
| `/api/v1/search/suggest` | GET | 搜索建议 |
| `/api/v1/search/advanced` | POST | 高级搜索 |
| `/api/v1/search/history` | GET | 搜索历史 |
| `/api/v1/search/history/{id}` | DELETE | 删除历史 |
| `/api/v1/search/hot` | GET | 热门搜索 |

---

## 五、非功能性设计

| 指标 | 目标 | 方案 |
|:---|:---|:---|
| 搜索响应 | ≤ 500ms | Elasticsearch 优化 |
| 建议响应 | ≤ 200ms | 前缀缓存 |
| 高亮展示 | ≤ 100ms | 异步加载 |

---

🦾 *"我是天才，技术方案完成！"*