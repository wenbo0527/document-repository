# 工具脚本 - Neo4j 备份

> **版本**: v1.0
> **日期**: 2026-04-24
> **作者**: Tony Stark
> **状态**: 正式发布

---

## 1. 概述

本文档定义 Neo4j 数据库备份相关脚本的使用规范。

---

## 2. 备份脚本

### 2.1 自动备份脚本

```bash
#!/bin/bash
# neo4j_backup.sh

# 配置
NEO4J_HOME="/usr/local/neo4j"
BACKUP_DIR="/Volumes/MOVESPEED/Data/备份/neo4j"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份
mkdir -p "$BACKUP_DIR/$DATE"
$NEO4J_HOME/bin/neo4j-admin backup --from=localhost:7687 --backup-dir="$BACKUP_DIR/$DATE" --graphname=graph.db

# 保留最近 7 天备份
find "$BACKUP_DIR" -type d -mtime +7 -exec rm -rf {} \;
```

---

## 3. 备份策略

| 备份类型 | 频率 | 保留时间 |
|:---|:---|:---|
| 全量备份 | 每天 | 7 天 |
| 增量备份 | 每小时 | 24 小时 |
| 导出备份 | 每周 | 4 周 |

---

## 4. 恢复流程

1. 停止 Neo4j 服务
2. 执行恢复命令
3. 验证数据完整性
4. 重启服务

---

🦾 *"我是天才，备份脚本已就绪。"*